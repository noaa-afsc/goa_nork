# Figures

```{r fcatch, fig.cap="Observed and estimate commercial catch of GOA northern rockfish in the Gulf of Alaska from the 2024 model (m24).", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "fcatch.png"))
```

```{r fcatch1, fig.cap="Annual Gulf of Alaska northern rockfish cumulative catch by Julian day. The dot indicates catch in the current year.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "fcatch1.png"))
```


```{r ffsc, fig.cap="Annual fishery length composition plots. Bars are observed values, lines are estimated values.", eval = T}

knitr::include_graphics(here::here(year,  pref_mod, "figs", "fsc.png"))
```

```{r ffac, fig.cap="Annual fishery age composition plots. Bars are observed values, lines are estimated values.", eval = T}

knitr::include_graphics(here::here(year,  pref_mod, "figs", "fac.png"))
```

```{r fsrvbio, fig.cap="Observed (geostatistical model-based estimates) and predicted GOA northern rockfish trawl survey biomass based on the 2024 recommended model. Error bars are approximate asymptotic 95% confidence intervals of model error, the shaded area is the 95% confidence intervals for the predicted biomass.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "srv_biomass.png"))
```

```{r fcpue, fig.cap="Spatial distribution of northern rockfish in the Gulf of Alaska during the last three NMFS trawl surveys.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "sp_srv_bio.png"))
```

```{r fsac, fig.cap="Annual survey age composition plots. Bars are observed values, lines are estimated values.", eval = T}

knitr::include_graphics(here::here(year,  pref_mod, "figs", "sac.png"))
```

```{r fslx, fig.cap="GOA northern rockfish estimated fishery and survey selectivities for the 2024 model and maturity estimates from the ADMB model and a generalized linear model with updated maturity information.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "selex.png"))
```

```{r fupdate-mat, fig.cap="GOA northern rockfish estimated fishery and survey selectivities for the 2024 model and maturity estimates from the ADMB model and a generalized linear model with updated maturity information.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "update_maturity1.png"))
```


```{r frecr-ssb, fig.cap = "Scatterplot of spawner-recruit estimates for the GOA northern rockfish author's recommended model for 20224.", eval=T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "recr-ssb.png"))
```

```{r fbiomass, fig.cap = "Time series of estimated female spawning and age 2+ total biomass for GOA northern rockfish from the 2024 model (m24), the 2022 SSC accepted model with 2022 data (base) and 2024 data (m22.1). The 95\\% credible intervals are from m24.", eval=T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "est_biomass.png"))
```

```{r fphase, fig.cap="Time series of northern rockfish estimated spawning biomass (SSB) relative to B~35%~ and fishing mortality (F) relative to F~35%~ for author recommended model.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "phase_plane.png"))
```

```{r fF, fig.cap="Time series of estimated fully selected fishing mortality for GOA northern rockfish from the 2024 model (m24), the 2022 SSC accepted model with 2022 data (base) and 2024 data (m22.1). The 95\\% credible intervals are from m24.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "Ft.png"))
```

```{r fcatch-bio, fig.cap="Time series of estimated Gulf of Alaska northern rockfish catch/age 2+ biomass ratio with approximate 95% confidence intervals. Observed catch values were used for 1990-2024, the 2024 catch values were estimated using an expansion factor. The horizontal dashed line is the mean value for the entire dataset.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "catch_bio.png"))
```

```{r frecruit, fig.cap="Time series of estimated age-2 GOA northern rockfish recruitment from the 2024 model (m24), the 2022 SSC accepted model with 2022 data (base) and 2024 data (m22.1). The 95% credible intervals are from m24.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "recruits.png"))
```

```{r fac-resid, fig.cap="Fishery age composition Pearson and one-step ahead residual plots (top of panel). Aggregate composition plot (observed = gray bars, estimated = black line), and sample sizes (lower panel). Outliers are residuals that are >3, effN is effective sample size, ISS is the input sample size, SDNR is the standard deviation of normalized residuals.", eval = T}

knitr::include_graphics(here::here(year,  pref_mod, "figs", "fac_resid.png"))
```


```{r sac-resid, fig.cap="Survey age composition Pearson and one-step ahead residual plots (top of panel). Aggregate composition plot (observed = gray bars, estimated = black line), and sample sizes (lower panel). Outliers are residuals that are >3, effN is effective sample size, ISS is the input sample size, SDNR is the standard deviation of normalized residuals.", eval = T}

knitr::include_graphics(here::here(year,  pref_mod, "figs", "sac_resid.png"))
```

```{r fsc-resid, fig.cap="Fishery length composition Pearson and one-step ahead residual plots (top of panel). Aggregate composition plot (observed = gray bars, estimated = black line), and sample sizes (lower panel). Outliers are residuals that are >3, effN is effective sample size, ISS is the input sample size, SDNR is the standard deviation of normalized residuals.", eval = T}

knitr::include_graphics(here::here(year,  pref_mod, "figs", "fsc_resid.png"))
```


```{r fhist, fig.cap="Histograms of estimated posterior distributions for key parameters derived (or estimated, in the case of *q*) from the MCMC for GOA northern rockfish. Vertical black lines represent the maximum likelihood estimate for comparison with the MCMC results.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "hists.png"))
```

```{r fswath, fig.cap="Median northern rockfish spawning stock biomass from MCMC simulations with Bayesian credible intervals including projections for 2025-2039, when managing under Scenario 2. Assumes the same average yield ratio forward in time. Dotted horizontal line is $B_{40\\%}$ and solid horizontal line is $B_{35\\%}$ based on recruitments from 1977-2022. Each shade is 5% of the posterior distribution.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "swath.png"))
```

```{r ssb-retro, fig.cap="Retrospective peels of estimated female spawning biomass for the past 10 years from the recommended model with 95% credible intervals derived from MCMC (upper panel) and the percent difference from the terminal year estimates (lower panel).", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "ssb_retro.png"))
```

```{r fpast, fig.cap="Gulf of Alaska northern rockfish spawning biomass estimates from the 2009-2024 Stock Assessment and Fishery Evaluation reports.", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "past_ssb.png"))
```

```{r tot-retro, fig.cap="Retrospective peels of estimated total biomass for the past 10 years from the recommended model with 95% credible intervals derived from MCMC (upper panel) and the percent difference from the terminal year estimates (lower panel).", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "tot_bio_retro.png"))
```

```{r rec-retro, fig.cap="Retrospective peels of estimated recruitment for the past 10 years from the recommended model with 95% credible intervals derived from MCMC (upper panel) and the percent difference from the terminal year estimates (lower panel).", eval = T}

knitr::include_graphics(here::here(year, pref_mod, "figs", "rec_retro.png"))
```









